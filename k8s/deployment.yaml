apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-app
  namespace: ecommerce
spec:
  replicas: 2
  selector:
    matchLabels:
      app: simple-app
  template:
    metadata:
      labels:
        app: simple-app
    spec:
      containers:
      - name: app
        image: sadanand25/simple-app:9578d2da5ddf3275db1f9a7863816d0e3d9960e7
        ports:
        - containerPort: 3000
        readinessProbe:
          httpGet:
            path: /health
            port: 3000

---
apiVersion: v1
kind: Service
metadata:
  name: simple-app-service
spec:
  selector:
    app: simple-app
  ports:
  - port: 80
    targetPort: 3000
  type: ClusterIP            