apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: production
data:
  DB_HOST: database
  DB_NAME: myapp
  DB_USER: postgres
  BACKEND_URL: http://backend:5000
  FRONTEND_URL: http://frontend:3000
  NODE_ENV: production